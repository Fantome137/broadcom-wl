[general]
name = broadcom-wl
version = 6.30.223.271
strip = yes
make = make
initramfs = yes

[hooks]
build = KBUILD_NOPEDANTIC=1 ${MAKE} ${CKMS_MAKE_ARGS} -C "${CKMS_KERNEL_HDRDIR}" M="${PWD}" -j${CKMS_JOBS}

[module.wl]
path = .
dest = kernel/drivers/net/wireless
